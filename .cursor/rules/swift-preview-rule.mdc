---
globs: *.swift
alwaysApply: false
---

# Swift 文件预览代码规则

每个 Swift 文件底部都必须添加预览代码

## 规则说明

- 所有 Swift 文件（包括 SwiftUI 视图文件）都必须在文件末尾添加预览代码
- 预览代码分为两部分：
  1. **整个App的预览**：使用 `ContentView().inRootView()`，用于查看整体应用界面
  2. **当前文件相关的预览**：使用当前文件中的视图（如 `MyView().inRootView()`），用于查看当前文件的具体界面
- 预览尺寸默认为 600x600，可以根据实际需要调整
- 预览代码应该放在文件的最后，在所有其他代码之后

## 示例

### 视图文件示例

```swift
import SwiftUI

struct MyView: View {
    var body: some View {
        Text("Hello World")
    }
}

// MARK: - Preview
#Preview("App") {
    ContentView()
        .inRootView()
        .frame(width: 600, height: 600)
}

#Preview("MyView") {
    MyView()
        .inRootView()
        .frame(width: 600, height: 600)
}
```

### 其他文件示例

对于非视图文件（如 Model、Service 等），可以只添加整个App的预览：

```swift
import SwiftUI

struct MyModel {
    let name: String
}

// MARK: - Preview
#Preview("App") {
    ContentView()
        .inRootView()
        .frame(width: 600, height: 600)
}
```

## 注意事项

- 预览代码应该简洁明了，便于开发者快速预览
- 如果文件包含多个视图，可以为每个视图添加单独的预览
- 预览尺寸可以根据视图的实际需求进行调整，600x600 仅为默认值，不是强制要求
- 整个App的预览使用 "App" 作为名称，当前文件的预览使用视图名称作为预览名称
